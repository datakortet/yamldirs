language: python

sudo: false

python:
  - "2.7"
  # - "3.4"
  - "3.5"
  - "3.6"
  - "3.7"

install:
  - pip install -r requirements.txt
  - pip install -e .
  - pip install coveralls

script:
  py.test -v --cov=yamldirs .

after_success:
  coveralls
