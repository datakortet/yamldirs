_schema: https://static.datakortet.no/schema/dkbuild.schema.yaml

package:
  name: yamldirs
  description: create directories and files (incl. contents) from yaml spec.
  version: 1.1.12
  created: 2015

build:
  venv: yamldirs
